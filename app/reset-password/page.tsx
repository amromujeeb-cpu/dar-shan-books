"use client";
import {useState} from "react";
import {useRouter} from "next/navigation";
import SiteNav from "@/components/SiteNav";
import SiteFooter from "@/components/SiteFooter";
import {supabase} from "@/lib/supabase";
export default function ResetPassword(){const router=useRouter();const[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget),password=String(d.get("password")),confirm=String(d.get("confirm"));if(password.length<8)return setError("كلمة المرور يجب أن تكون 8 أحرف على الأقل");if(password!==confirm)return setError("كلمتا المرور غير متطابقتين");setBusy(true);const{error}=await supabase.auth.updateUser({password});setBusy(false);if(error)return setError("انتهت صلاحية الرابط. اطلب رابطًا جديدًا.");router.replace("/login?reset=1")}return <><SiteNav/><main className="max-w-md mx-auto px-6 py-16"><form onSubmit={submit} className="surface border border-line rounded-xl p-8 shadow-sm"><h1 className="text-2xl font-extrabold mb-2">كلمة مرور جديدة</h1><p className="text-sm text-muted mb-7">اختر كلمة مرور قوية من 8 أحرف على الأقل.</p>{error&&<div className="bg-red/10 text-red p-3 rounded mb-4">{error}</div>}<input required minLength={8} type="password" name="password" placeholder="كلمة المرور الجديدة" className="w-full border rounded p-3 mb-4"/><input required minLength={8} type="password" name="confirm" placeholder="تأكيد كلمة المرور" className="w-full border rounded p-3 mb-4"/><button disabled={busy} className="w-full bg-purple text-white py-3 rounded font-bold">{busy?"جاري الحفظ…":"حفظ كلمة المرور"}</button></form></main><SiteFooter/></>}
