"use client";
import {useState} from "react";
import Link from "next/link";
import SiteNav from "@/components/SiteNav";
import SiteFooter from "@/components/SiteFooter";
import {useAuth} from "@/lib/auth-context";
export default function ForgotPassword(){const{requestPasswordReset}=useAuth();const[email,setEmail]=useState(""),[error,setError]=useState(""),[sent,setSent]=useState(false),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await requestPasswordReset(email);setBusy(false);if(!r.ok)return setError(r.error||"");setSent(true)}return <><SiteNav/><main className="max-w-md mx-auto px-6 py-16"><div className="surface border border-line rounded-xl p-8 shadow-sm">{sent?<div className="text-center"><div className="w-14 h-14 mx-auto mb-5 rounded-full bg-green/10 text-green grid place-items-center text-2xl">✓</div><h1 className="text-2xl font-extrabold mb-3">تفقد بريدك الإلكتروني</h1><p className="text-sm text-muted mb-6">أرسلنا رابط تغيير كلمة المرور إلى <b dir="ltr">{email}</b>. تفقد البريد غير المرغوب أيضًا.</p><Link href="/login" className="text-purple font-bold">العودة لتسجيل الدخول</Link></div>:<><h1 className="text-2xl font-extrabold mb-2">نسيت كلمة المرور؟</h1><p className="text-sm text-muted mb-7">أدخل بريد حسابك وسنرسل لك رابطًا آمنًا لتعيين كلمة مرور جديدة.</p>{error&&<div className="bg-red/10 text-red p-3 rounded mb-4">{error}</div>}<form onSubmit={submit} className="space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني" className="w-full border rounded p-3"/><button disabled={busy} className="w-full bg-purple disabled:opacity-60 text-white py-3 rounded font-bold">{busy?"جاري الإرسال…":"إرسال رابط الاسترجاع"}</button></form><p className="text-sm text-center mt-5"><Link href="/login" className="text-purple font-bold">العودة لتسجيل الدخول</Link></p></>}</div></main><SiteFooter/></>}
