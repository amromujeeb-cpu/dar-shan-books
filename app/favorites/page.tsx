"use client";import SiteNav from"@/components/SiteNav";import SiteFooter from"@/components/SiteFooter";import BookCard from"@/components/BookCard";import{useStore}from"@/lib/store-context";import{useExperience}from"@/lib/experience-context";export default function Favorites(){const{books}=useStore(),{favorites}=useExperience(),shown=books.filter(b=>favorites.includes(b.id));return <><SiteNav/><main className="max-w-6xl mx-auto px-5 md:px-12 py-12"><h1 className="text-3xl font-extrabold mb-8">المفضلة</h1>{shown.length?<div className="grid grid-cols-2 md:grid-cols-4 gap-5">{shown.map(b=><BookCard key={b.id} book={b}/>)}</div>:<div className="border rounded-xl p-16 text-center text-gray-400">لم تضيفي كتبًا إلى المفضلة بعد.</div>}</main><SiteFooter/></>}
