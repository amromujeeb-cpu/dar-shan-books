"use client";import SiteNav from"@/components/SiteNav";import SiteFooter from"@/components/SiteFooter";import BookCard from"@/components/BookCard";import{useStore}from"@/lib/store-context";export default function Author({params}:{params:{name:string}}){const name=decodeURIComponent(params.name),{books}=useStore(),shown=books.filter(b=>b.author===name);return <><SiteNav/><main className="max-w-6xl mx-auto px-6 py-12"><p className="text-purple font-bold mb-2">مؤلف</p><h1 className="text-3xl font-extrabold mb-3">{name}</h1><p className="text-gray-500 mb-9">جميع الكتب المتاحة لهذا المؤلف في دار شأن.</p><div className="grid grid-cols-2 md:grid-cols-4 gap-5">{shown.map(b=><BookCard key={b.id} book={b}/>)}</div></main><SiteFooter/></>}
